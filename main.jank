let a = 10;
let b = 20.5;
let c = a + b;

fn add(a, b) {
    return a + b;
}

fn void() {
    return;
}

fn main() {
    // Comment
    println("Hello");
    println(add(a, b), "should be equal to", c);

    // void
    void();

    return 69;
}